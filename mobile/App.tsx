/**
 * Runtime entry is `index.ts` → `expo-router/entry` (see package.json "main").
 * Screens live under `app/`. This file is not mounted in production builds.
 */
export default function App() {
  return null;
}
